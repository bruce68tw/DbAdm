@*using Resources*@

@{
    /*
    var locales = new List<IdStrDto>()
    {
        //Id need map to locale !!
        new IdStrDto(){ Id = "zh-TW", Str = "Taiwan" },
        new IdStrDto(){ Id = "en-US", Str = "English" },
        new IdStrDto(){ Id = "zh-CN", Str = "China" },
    };
    */
}
<style>
    #_Top {
        color: #EAFFFF;
        background: #28C2FE;
        height: 45px;
    }
</style>

<nav id="_Top" class="navbar navbar-expand navbar-dark">
    <a class="sidebar-toggle text-light mr-3" onclick="_leftmenu.onToggleMenu(); return false;" style="cursor:pointer">
        <!--<i class="fa fa-bars"></i>-->
        <span class="navbar-toggler-icon"></span>
    </a>

    <a class="navbar-brand" href="#"><i class="fa fa-code-branch"></i>資料庫文件系統</a>

    @*
        <div class="navbar-collapse collapse">
            <ul class="navbar-nav ml-auto">
                <li>
                    @Html.XiSelect("_Locale", _Session.Locale, locales, prop: new PropSelectDto() { AddEmptyRow = false, FnOnChange = "_xp.onChangeLocale(this.value)" })
                </li>
                <li style="margin:auto; padding-right:10px; font-size: 20px;">
                    <a class="nav-link" href="@Url.Action("Board", "Home")">
                        <i class="fa fa-home"></i>
                    </a>
                </li>
                <li style="margin:auto; padding-right:10px;">
                   @_Session.UserName
                </li>
                <li>
                    <a class="nav-link" href="@Url.Action("Login", "Home")">
                        <i class="fa fa-sign-out"></i>Logout
                    </a>
                </li>
            </ul>
        </div>
    *@
</nav>

