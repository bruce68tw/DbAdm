@inject IHtmlLocalizer<DbAdm.R0> R

<script src="~/js/view/MyCrud.js"></script>
<script type="text/javascript">
    $(function () {
        _me.init();
    });
</script>

@await Component.InvokeAsync("XgProgPath", new { names = new string[] { R["MenuCrud"].Value } })
<div class="xp-prog">
    <!-- read form-->
    <div id="divRead">
        <!-- condition -->
        <form id='rform' class='xg-form' style="margin-bottom:0;">
            <div class="row">
                @await Component.InvokeAsync("XiSelect", new { title = R["Project"].Value, fid = "ProjectId2", rows = (List<IdStrDto>)ViewBag.Projects, inRow = true })
                @await Component.InvokeAsync("XgFindTbar")
            </div>
            @await Component.InvokeAsync("XiText", new { title = R["ProgCode"].Value, fid = "ProgCode" })
        </form>

        <!-- other functions -->
        <div class="xg-btns-box" style="margin-top:5px;">
            @await Component.InvokeAsync("XgCreate", new { fnOnClick = "_me.onCreate()" })
            <button type="button" class="btn xg-btn" onclick="_me.onGenCrud()">@R["GenCrud"]</button>
        </div>

        <!-- result list -->
        <table id="table1" class="table table-bordered xg-table" cellspacing="0">
            <thead>
                <tr>
                    <th width="60px" class="xg-center">@R["Select"]</th>
                    <th>@R["Project"]</th>
                    <th>@R["ProgCode"]</th>
                    <th width='80px'>@R["Crud"]</th>
                    <th width='60px' class="xg-center">@R["Status"]</th>
                    <th width='100px'>@R["Created"]</th>
                </tr>
            </thead>
            <tbody></tbody>
        </table>
    </div>

    <!-- edit form-->
    <div id="divEdit" class="xg-hide">
        <partial name="Edit" />
    </div>
</div>