import { _Ajax, _Form, _Json, _iSelect } from "@baseJs";
_me = {
  init: function() {
    _me.rform = $("#formRead");
  },
  //on select bao item
  onFind: async function() {
    var data = _Json.toStr(_Form.toRow(_me.rform));
    await _Ajax.getJsonA("GetData", { json: data }, function(row) {
      var rowLen = row["Rows"];
      var config = { title: `\u6EFF\u610F\u5EA6\u554F\u5377\u7D71\u8A08-\u5171 ${rowLen} \u7B46\u8CC7\u6599` };
      config.labels = ["Q1", "Q2", "Q3", "Q4"];
      config.values = [];
      var rows = _Json.toChartRows(row, config.labels);
      for (var i = 0; i < rows.length; i++) {
        config.values[i] = parseFloat((rows[i]["Num"] / rowLen).toFixed(1));
      }
    });
  },
  //on change projectId
  onChgProject: function() {
    _iSelect.changeParent("_ProjectId", "ProgId", "", "GetPrjProgs", false);
  }
};
$(function() {
  _me.init();
});
//# sourceMappingURL=ChartSatis.min.js.map
