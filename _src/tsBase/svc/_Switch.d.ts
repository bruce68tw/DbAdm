export default class _Switch {
    /**
     * 傳回元件內容字串 for client render
     */
    static getText(yes: string, no: string, width: number | string, status: boolean | number, inline?: boolean, fid?: string, cls?: string): string;
}
